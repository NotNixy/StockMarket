# Streamlit Community Cloud reads this from the REPOSITORY ROOT (or from the
# folder holding the entrypoint file). Anywhere else and it installs nothing
# but streamlit, which fails at the first `from scipy import stats`.
#
# pytest is omitted deliberately -- the deployed app never runs the suite, and
# every package listed here is one more thing that can fail to build.

pandas==2.2.3
numpy==2.1.3
scipy==1.14.1
matplotlib==3.9.3
pyarrow==18.1.0
yfinance==0.2.51
streamlit==1.41.1
