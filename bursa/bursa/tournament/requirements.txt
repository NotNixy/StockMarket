# Deployment dependencies, deliberately duplicated here.
#
# Streamlit Community Cloud looks for a requirements file in exactly two
# places: the folder holding the entrypoint (this one) or the repository root.
# A copy here means the app installs correctly no matter how deeply the repo
# is nested inside the GitHub project -- which is what broke the first deploy.
#
# pytest and scikit-learn are omitted: the deployed app runs neither, and every
# package listed is one more that can fail to build on the Cloud image.
#
# Keep in step with ../requirements.txt when versions change.

pandas==2.2.3
numpy==2.1.3
scipy==1.14.1
matplotlib==3.9.3
pyarrow==18.1.0
yfinance==0.2.51
streamlit==1.41.1
